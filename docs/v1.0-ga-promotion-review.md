# BLUE-TANUKI v1.0 GA Promotion Review

Decision state: `PENDING_OWNER_GO`

Machine gate:

```bash
pnpm validate:ga
```

Current gate result:

```txt
status=pre_go_ready
owner_go=pending
public_claim_allowed=false
```

## Boundary

This review is the S13 pre-GO promotion evidence pack. It does not promote the
workspace to `1.0.0`, does not activate external public claims, and does not
change README / QUICKSTART / CLAIM into GA launch copy.

Actual GA promotion requires an explicit owner GO decision after the owner
reviews the GA bar evidence and technical validation.

## Bar Snapshot

| Bar | Status | Evidence |
|---|---|---|
| A: Authority and Safety Closure | PASS | `docs/v1.0-security-and-permanent-use-review.md`, HDS-BRAIN approval policy, compound attack tests, fail-safe tests |
| B: First-Party Surface Closure | PASS | Writing / Daily / Developer Operator docs and packages |
| C: Installer and Setup Experience | PASS | `docs/INSTALLER_GUIDE.md`, portable install docs, Settings `Verify LLM` |
| D: Resident Application Experience | PASS | `docs/RESIDENT_APP_GUIDE.md`, resident helper docs, portable launcher lifecycle |
| E: Channel Selection Completeness | PASS | `pnpm validate:channels`, compatibility matrix, WhatsApp reserved-third-party boundary |
| F: Platform Extension Surface | PASS | Plugin Review Gate, Plugin HIG, Skill Loader Contract, `pnpm plugin:review` |
| G: Public Claim Eligibility | PENDING_OWNER_GO | Bar A-F pass evidence exists, but owner GO is not recorded |

## Owner GO Evidence

Owner GO is not inferred from implementation progress. A future actual
promotion must record a reviewable owner decision before `1.0.0` version
promotion or public claim activation.

Expected decision file shape:

```json
{
  "schema_version": 1,
  "decision": "GO",
  "version": "1.0.0",
  "decided_at": "2026-05-19T00:00:00.000Z",
  "ga_bar_reviewed": true,
  "technical_validation_reviewed": true,
  "public_claim_authorized": true
}
```

The file path used by the machine gate is `docs/ga-owner-decision.json`.

## Public Claim Boundary

Until owner GO is recorded:

- `package.json` remains `1.0.0-rc.1`
- release artifact naming remains RC
- README / QUICKSTART / CLAIM must not activate public GA launch claims
- `public_claim_allowed=false`

## Technical Validation Set

The S13 pre-GO gate is not a replacement for technical validation. The expected
validation set remains:

- `pnpm install --frozen-lockfile`
- `pnpm typecheck`
- `pnpm build`
- `pnpm test`
- `pnpm docs:check`
- `pnpm validate:packaging`
- `pnpm validate:channels`
- `pnpm plugin:review -- --package packages/channel-slack --bundled`
- `pnpm validate:ga`
- `pnpm smoke:live`
- `pnpm run doctor`
- `pnpm release:bundle -- --dry-run`
- `pnpm release:bundle`
- `pnpm release:verify`
- `pnpm hds:standalone`
- `pnpm smoke:serve`
- `pnpm smoke:resume`

## Next Action

Owner reviews this evidence. If the decision is GO, a separate promotion block
may update the workspace version to `1.0.0`, activate approved public claim
language, regenerate the release bundle, and rerun the full validation set with
`pnpm validate:ga -- --require-owner-go`.
