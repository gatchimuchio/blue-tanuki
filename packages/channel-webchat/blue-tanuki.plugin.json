{
  "name": "@blue-tanuki/channel-webchat",
  "version": "0.0.2",
  "kind": "channel",
  "entry": "./dist/index.js",
  "exports": {
    "channel": "WebChatChannel"
  },
  "permissions": [
    "network:listen",
    "secrets:WEBCHAT_TOKEN",
    "secrets:WEBCHAT_RESUME_TOKEN",
    "secrets:WEBHOOK_TOKEN",
    "secrets:BLUE_TANUKI_SETTINGS_TOKEN"
  ],
  "description": "HTTP+WS WebChat channel. Bearer auth, /ws-ticket single-use authentication, /healthz, /resume with request-bound one-time approval tokens, optional /webhook with a dedicated webhook token, /settings with dedicated settings-token auth, per-key token-bucket rate limiting. Listens on the configured port."
}
