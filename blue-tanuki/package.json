{
  "name": "@blue-tanuki/core",
  "version": "0.0.1",
  "private": true,
  "description": "BLUE-TANUKI executor: runs commands from HDS-BRAIN. LLM abstraction, tools, channels.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@blue-tanuki/protocol": "workspace:*"
  }
}
